todo уточнить, потому что здесь описана какая-то дичь
Статистика по окончаниям существительных

Слова на -ი (около 50–55% всех слов): Самая большая группа.
У них окончание именительного падежа -ი полностью заменяется на -ის (კაცი -->> კაცის, სახლი -->> სახლის).
Простое добавление ს здесь даст 100% ошибку (получится несуществующее სახლის).

Слова на -ა (около 30–35% всех слов): Самая стабильная группа для вашего алгоритма.
Почти все слова на -ა образуют родительный падеж простым добавлением ს в конец (დედა -->> დედას, მამა -->> მამას).

Слова на -ე (около 10% всех слов):
Здесь ваше правило сработает только для имен собственных и заимствований (კაფე -->> კაფეს).
Нарицательные слова усекаются (ხე -->> ხის).

Слова на -ო и -უ (около 3–5% всех слов):
К ним в родительном падеже добавляется окончание -ს,
но перед ним обязательно вставляется буква ვ
(წყარო -->> წყაროს / წყაროვის,
კენგურუ -->> კენგურუს / კენგურუვის — в современном языке допустимы оба варианта, но классический требует -ვის).


Как легко улучшить точность программы до 80%+Вместо простого добавления ს к любому слову,
разбейте логику на три базовых правила проверки последней буквы:
Если слово оканчивается на -ი: Отрежьте ი и добавьте ის (სახლი -->> სახლ + ის).
Если слово оканчивается на -ა: Просто добавьте ს (დედა -->> დედა + ს).
Если слово оканчивается на -ო или -უ: Добавьте ვის (წყარო -->> წყარო + ვის).

Предлженый женей вариант
---СТАРТ----
fun toGeorgianGenitive(noun: String): String {
    if (noun.isBlank()) return noun

    val word = noun.trim()
    val lastChar = word.last()

    return when (lastChar) {
        // 1. Именительный на -ი (Самая большая группа)
        'ი' -> {
            val stem = word.dropLast(1)
            // Реализация "усечения" (квеца) для основы на -ელ / -ალ / -ონ / -ინ
            // Например: მგელი -> მგლის, ქართველი -> ქართველის, ფანჯარა -> ფანჯრის
            if (stem.length > 2) {
                val penultimate = stem[stem.length - 2]
                if (penultimate == 'ე' || penultimate == 'ა' || penultimate == 'ო') {
                    // Вырезаем эту гласную перед последней согласной
                    return stem.substring(0, stem.length - 2) + stem.last() + "ის"
                }
            }
            stem + "ის"
        }

        // 2. Именительный на -ა
        'ა' -> word + "ს"

        // 3. Именительный на -ე
        'ე' -> {
            // Если это короткое исконное слово (მზე, ხე, მეფე) -> усекается 'ე' и добавляется 'ის'
            // Если длинное заимствование (კაფე, ატელიე) или имя собственное -> просто + 'ს'
            if (word.length <= 4) {
                word.dropLast(1) + "ის"
            } else {
                word + "ს"
            }
        }

        // 4. Именительный на -ო или -უ
        'ო', 'უ' -> word + "ვის"

        // На случай, если слово передано в виде основы без окончания падежа //todo это не надо
        else -> word + "ის"
    }
}
---КОНЕЦ----
